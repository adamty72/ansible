---
- name: Check Disk Space Usage
  hosts: 95.179.234.251
  tasks:
          - name: Disk usage from command module
            ansible.builtin.command: df -h
            register: space
          - ansible.builtin.debug:
                  var: space
